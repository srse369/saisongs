// Canvas display width (height calculated based on aspect ratio)
export const CANVAS_WIDTH = 640;

// UI Constants
export const ELEMENT_MARGIN = 40; // Margin from edges in slide coordinates
export const OVERLAY_MAX_ATTEMPTS = 5; // Max attempts to wait for stage to be ready
export const OVERLAY_ATTEMPT_DELAY = 50; // ms between attempts
export const VIDEO_LOAD_TIMEOUT = 3000; // ms to wait for video metadata
export const SCALE_INCREMENT = 0.1; // Zoom step size
export const MIN_SCALE = 0.1;
export const MAX_SCALE = 3.0;
