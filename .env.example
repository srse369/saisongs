# ======================================
# SongStudio Local Environment Variables
# ======================================

# ----------------------------------------
# Server Configuration
# ----------------------------------------
# Port for the backend Express server (default: 3001)
PORT=3001

# ----------------------------------------
# Frontend API Configuration
# ----------------------------------------
# Base URL for API calls from the frontend
# In local development, this points to your local backend server
VITE_API_URL=/api

# ----------------------------------------
# Oracle Database Configuration
# ----------------------------------------
# Oracle Autonomous Database credentials
# Get these from your Oracle Cloud Console
VITE_ORACLE_USER=dbuser
VITE_ORACLE_PASSWORD=dbpassword

# Oracle connection string format:
# (description=(retry_count=20)(retry_delay=3)(address=(protocol=tcps)(port=1522)(host=your-host.oraclecloud.com))(connect_data=(service_name=your_service_name))(security=(ssl_server_dn_match=yes)))
VITE_ORACLE_CONNECT_STRING=songstudiodb_medium

# ----------------------------------------
# Application Security
# ----------------------------------------
# Admin password for accessing protected admin routes
VITE_ADMIN_PASSWORD=adminpassword
VITE_EDITOR_PASSWORD=editorpassword

# ----------------------------------------
# Deployment Configuration
# ----------------------------------------
# Deployment target: 'vps' for VPS deployment, 'github' for GitHub Pages
# This affects the base path used in the build
VITE_DEPLOY_TARGET=vps

# ----------------------------------------
# Additional Notes
# ----------------------------------------
# 1. Never commit this file to version control (it's gitignored)
# 2. All VITE_* variables are exposed to the frontend bundle
# 3. PORT is only used by the backend server
# 4. For Oracle setup, see: /database/README.md

# Oracle Wallet Configuration (required for Autonomous Database)
TNS_ADMIN=locationofwallet
VITE_ORACLE_WALLET_PASSWORD=walletpassword

# For remote deployments, have the following as well
# SSH username for the remote server
REMOTE_USER=remoteserveruser

# Hostname or domain of the remote server
REMOTE_HOST=remoteserver

# IP address of the remote server (optional, used for documentation)
REMOTE_IP=remoteserverip

# Installation path on the remote server
REMOTE_PATH=/var/www/songstudio

# Path to SSH private key (optional)
SSH_KEY=pathtosshkeytoconnecttoremoteserver

