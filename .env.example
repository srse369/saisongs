# ============================================
# Sai Songs Environment Configuration
# ============================================
# Copy this file to .env.local for local development
# For GitHub Actions deployments, add these as secrets in GitHub Environments
# ============================================

# --------------------------------------------
# Oracle Database Connection
# --------------------------------------------
# Required for all database operations
ORACLE_USER=your_oracle_username
ORACLE_PASSWORD=your_oracle_password
ORACLE_CONNECT_STRING=your_tns_connection_string

# Oracle Wallet (required if using Oracle Cloud)
# Path to wallet directory - usually ./wallet in project root
TNS_ADMIN=./wallet
ORACLE_WALLET_PASSWORD=your_wallet_password

# Oracle Instant Client library path (production servers only)
# Example: /opt/oracle/instantclient_21_13:/usr/lib
# LD_LIBRARY_PATH=/opt/oracle/instantclient_21_13

# --------------------------------------------
# Authentication
# --------------------------------------------
# Session secret for cookie signing (use a long random string in production)
SESSION_SECRET=your-secure-session-secret-min-32-chars

# Role-based access passwords
VITE_ADMIN_PASSWORD=your_admin_password
VITE_EDITOR_PASSWORD=your_editor_password

# --------------------------------------------
# Email Service (Brevo/Sendinblue)
# --------------------------------------------
# Required for OTP email authentication
BREVO_API_KEY=your_brevo_api_key
BREVO_SENDER_EMAIL=noreply@yourdomain.com
BREVO_SENDER_NAME=Sai Songs

# --------------------------------------------
# Server Configuration
# --------------------------------------------
# Node environment (development | production)
NODE_ENV=development

# Server port (default: 3111)
PORT=3111

# Frontend URL for CORS (local dev)
FRONTEND_URL=http://localhost:5111

# API URL for frontend (local dev uses proxy, production uses /api)
VITE_API_URL=http://localhost:3111/api

# --------------------------------------------
# Cookie Configuration (Production)
# --------------------------------------------
# Set to 'true' only if using HTTPS
COOKIE_SECURE=false

# Cookie domain for production (optional)
# COOKIE_DOMAIN=yourdomain.com

# --------------------------------------------
# PowerPoint Media Storage
# --------------------------------------------
# Directory to store extracted PowerPoint media files
# Default: ./public/pptx-media
# PPTX_MEDIA_DIR=/var/www/saisongs/public/pptx-media
